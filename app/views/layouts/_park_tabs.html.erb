<ul class="nav nav-tabs">
  <%= tab_generator("Index", marked_park_index_path(page: session[:page]), "fas fa-list") %>

  <%= tab_generator("Filter", marked_park_filter_path, "fas fa-filter") %>

  <%= tab_generator("Recheck Status", marked_park_status_path, 'fas fa-sync') %>

  <%= tab_generator("Autocompletion", marked_park_autocomplete_path, 'fas fa-magic') %>

  <%= tab_generator("Information", marked_park_path(params[:id]), "fas fa-info-circle") if params[:id].present? %>

  <%= tab_generator("Edit Information", edit_marked_park_path(params[:id]), "fas fa-screwdriver") if params[:id].present? %>

  <%= tab_generator("Resolve Differences", marked_park_quick_path(params[:id]), "fas fa-marker") if params[:id].present? && @park.editable? %>
</ul>
<ul class="nav nav-tabs">
  <%= tab_generator("Index", marked_park_index_path, "fas fa-list") %>

  <%= tab_generator("Recheck Status", marked_park_status_path, 'fas fa-sync') %>

  <%= tab_generator("Information", marked_park_path(params[:id]), "fas fa-info-circle") if params[:id].present? %>

  <%= tab_generator("Compare", nil, "fas fa-pencil-alt") if params[:action] == 'edit' %>

  <%= tab_generator("Quick Edit", marked_park_quick_path(params[:id]), "fas fa-marker") if params[:id].present? && @park.quick_edit? %>
</ul>
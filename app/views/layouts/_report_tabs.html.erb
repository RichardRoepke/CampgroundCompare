<ul class="nav nav-tabs">
  <%= tab_generator("Park Status", report_path(report: 'park_status')) %>
  <%= tab_generator("Editable Parks", report_path(report: 'park_editable')) %>
  <%= tab_generator("Kind of Differences", report_path(report: 'differences_kind')) %>
  <%= tab_generator("Differences Fields", report_path(report: 'differences_catalogue_field')) %>
</ul>